services:
  support-agent:
    build: .
    image: ai-customer-support-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Inside the container the mock backend is reached on localhost.
      BACKEND_BASE_URL: http://127.0.0.1:8000
    restart: unless-stopped
